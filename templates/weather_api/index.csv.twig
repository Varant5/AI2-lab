city,country,date,celsius,windSpeed,humidity,pressure
{{ "\n"|nl2br}}
{% for measurement in measurements %}
    {{ city }},{{ country }},{{ measurement.date|date('Y-m-d') }},{{ measurement.temperature }},{{ measurement.fahrenheit }},{{ measurement.windSpeed }},{{ measurement.humidity }},{{ measurement.pressure }}
    {% if not loop.last %}
        {{ "\n"|nl2br}}
    {% endif %}
{% endfor %}
